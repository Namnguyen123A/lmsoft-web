import * as React from 'react';
import { DataAwards } from "./Data-award"
import "./main.scss";

export interface ISlideProps {
}

export default function Footer (props: ISlideProps) {
  return (
    <div>
        
    </div>
  );
}
